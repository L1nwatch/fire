# Fire

Fire is a local-first portfolio sheet for monitoring financial independence, cash, and investment positions.

## Frontend

The app uses the same frontend stack as TriggerToDo:

- Vue 3
- TypeScript
- Vite
- Element Plus

Run it locally:

```bash
cd frontend
npm install
npm run dev
```

Build it:

```bash
cd frontend
npm run build
```
